# The built binary at repo root. Anchored with a leading slash so it does not
# also match the cmd/lyre/ source directory (an unanchored "lyre" matches any
# path component named lyre, which silently ignored new files under cmd/lyre/).
/lyre
pkg/extract/typescript/node_modules/
